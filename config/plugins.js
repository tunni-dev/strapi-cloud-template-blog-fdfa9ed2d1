module.exports = ({ env }) => ({
    i18n: {
      enabled: true,
      config: {
        defaultLocale: 'pt-BR',
        locales: ['pt-BR', 'en'], // Adicione outros idiomas conforme necessário
      },
    },
  });